package z18;

import java.awt.event.ActionListener;

public class menuOptions extends menu {
    public menuOptions(String name, int key, int modifier, ActionListener onPress) {
        super(name, key, modifier, onPress);
    }
}
