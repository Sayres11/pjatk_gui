/*package mypackage;

import jdk.jfr.Category;

import java.util.List;
import java.util.Vector;


public class Samolot {
    static Samolot.nazwaSamolot[] samolot = new Samolot.nazwaSamolot[10];


    public static class nazwaSamolot {
        public String name;
        protected int x, y, prendkosc;
        char Type;
        public String wektor;



        public nazwaSamolot(String name, int x, int y, int prendkosc, String wektor, char Type) {
            this.name = name;
            this.x = x;
            this.y = y;
            this.prendkosc = prendkosc;
            this.wektor = wektor;
            this.Type = Type;



     for (int i = 1; i < 10; i++) {

                samolot[i].x = (int) (Math.random() * (10 + 1) + 1);
                samolot[i].y = (int) (Math.random() * (10 + 1) + 1);
                samolot[i].prendkosc = (int) (Math.random() * (10 + 1) + 1);

            }




        }
    }
}*/

